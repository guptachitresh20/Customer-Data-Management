import { LogSearchFilterPipe } from './log-search-filter.pipe';

describe('LogSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LogSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
